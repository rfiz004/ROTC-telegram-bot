
services:
  - type: web
    name: rotc-telegram-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 main.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
    plan: free
